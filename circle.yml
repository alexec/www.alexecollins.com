#dependencies:
#  pre:
#  - sudo apt-get install -y gcc python-dev python-setuptools libffi-dev
#  - sudo apt-get install -y python-pip
#  - pip install gsutil
checkout:
  post:
  - bundler install
test:
  override:
  - true
general:
  artifacts:
  - build